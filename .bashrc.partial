
up() {
    local input="../"
    local count="${1:-1}"
    printf -v newpath '%*s' "$count"
    local path=$(printf '%s' "${newpath// /$input}")
    cd $path
}

src() {
    cd "$CODESPACE_VSCODE_FOLDER"
}
